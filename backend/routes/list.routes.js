const express = require('express');
const router = express.Router();

// Placeholder — full implementation in Part 3
// server.js imports this so it won't crash on startup during Part 1 testing

module.exports = router;
